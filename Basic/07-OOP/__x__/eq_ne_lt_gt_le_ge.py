# -*- coding:utf-8 -*-


# __eq__(self, other)      # 定义 == 运算符的行为   equal
# __ne__(self, other)      # 定义 != 运算符的行为   not equal
# __lt__(self, other)      # 定义 < 运算符的行为    less than
# __gt__(self, other)      # 定义 > 运算符的行为    great than
# __le__(self, other)      # 定义 <= 运算符的行为   less than or equal
# __ge__(self, other)      # 定义 >= 运算符的行为   great than or equal